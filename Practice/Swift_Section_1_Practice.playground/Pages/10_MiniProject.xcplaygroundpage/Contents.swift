// -------------------------------------------------------------
// MARK: - 7. Final Challenge
// -------------------------------------------------------------

/*:
 ## 🎯 Final Challenge — Console Interaction

 Write a function:

 ```swift
 func runApp()
 ```

 that simulates a tiny **console-based menu** for your profile manager.

 Suggested behavior:

 1. Print a welcome message.
 2. Show a menu, for example:

    ```
    1. List all profiles
    2. Add a new profile
    3. Toggle active status for a profile
    4. Quit
    ```

 3. Use `readLine()` to get user input from the console.
 4. Based on the choice:
    - Call helper functions that work with your `database` array.
    - Print out profile summaries using your existing methods and properties.
 5. Keep looping until the user chooses "Quit".

 Tips:
 - Start *simple* — even just handling "List all profiles" + "Quit" is a win.
 - You can grow it in stages as you get more comfortable.
 - Reuse as much code from Exercises 1–6 as possible so you’re not rewriting logic.

 When you’re ready, implement the function below and then call it at the bottom of the page.
 */

func runApp() {
    // 🧪 Start small:
    // 1. Print a welcome message
    // 2. Show a simple menu
    // 3. Read user input with `readLine()`
    // 4. Call functions that work with your `database` array
    // 5. Add a loop to keep the app running until the user quits

    print("👋 Welcome to the Swift Fundamentals Mini Profile Manager!")
    // TODO: Build out your menu + interaction here
}

// Uncomment this line when you're ready to test in a real console run:
// runApp()
